const assertEqual = require('./assertEqual');//facilitate importing assertEqual function

//function below returns the first element in the array
const head = function(array) {
  return array[0];
};

module.exports = head; //exporting head.js